def analyze_fundamentals(data):
    """
    Analyse fondamentale simplifiée.
    """
    # Exemple : Calcul du ratio P/E (à adapter)
    pe_ratio = data.get("PE Ratio", "N/A")
    return {"PE Ratio": pe_ratio}